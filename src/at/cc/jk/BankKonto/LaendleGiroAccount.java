package at.cc.jk.BankKonto;

public class LaendleGiroAccount extends Account {
    public LaendleGiroAccount(double debitInterest, double creditInterest, double minimumBalance) {
        super(debitInterest, creditInterest, minimumBalance);
    }
}
